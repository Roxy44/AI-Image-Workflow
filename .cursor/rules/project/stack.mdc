---
description: Project facts — stack, tooling, conventions, communication language, docs locale. Generated by the onboarding interview.
alwaysApply: true
---

# This project

Filled by `onboarding.mdc`. Keep it under ~30 lines: facts only, no explanations. If a
value still says `TBD`, run the onboarding interview before deep work (**M-1**).

Rule `.mdc` files stay English. Everything below about "language" is for humans and memory.

- **Communication language:** Russian
- **Docs locale:** ru (content unpacked into docs/)
- **UI / product copy:** Russian
- **Project type:** fullstack
- **Languages:** TypeScript
- **Frontend:** React, Vite, FSD, Tailwind, Zustand, TanStack Query, xyflow
- **Backend:** Node.js, Fastify, in-memory jobs (no DB, no ORM)
- **CSS naming:** n/a
- **Package manager:** yarn (Classic 1.22)
- **Lockfiles in git:** ignore
- **Lint / format:** ESLint + Prettier
- **Tests:** Vitest; cover graph engine, jobs, typed ports before "done"
- **Verify command(s):** yarn lint && yarn test && yarn typecheck && yarn build
- **Branching / commits:** GitHub Flow, Conventional Commits; agent never commits or pushes (G-1, G-5)
- **Hard constraints:** API key only on backend; FSD (no business logic in random UI); no canvas engine from scratch; Preset is a data-model entity; yarn only for installs; no secrets in repo

## Layout

- Frontend code: apps/web/src (FSD)
- Backend code: apps/api/src
- Shared: packages/shared
- Tests: colocated Vitest next to graph/job modules
- Docs root: docs
